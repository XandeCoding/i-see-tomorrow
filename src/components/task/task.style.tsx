import { StyleSheet } from 'react-native'

export const TaskStyle = StyleSheet.create({
    body: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center'
    }
})
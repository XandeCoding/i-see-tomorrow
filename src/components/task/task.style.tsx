import { StyleSheet } from 'react-native'

export const TaskStyle = StyleSheet.create({
  body: {
    width: '100%',
    justifyContent: 'center',
  },
  pressContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
  }
})
import { StyleSheet } from 'react-native'

export const TaskStyle = StyleSheet.create({
    body: {
        justifyContent: 'center',
    },
    pressContainer: {
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'row',
    }
})
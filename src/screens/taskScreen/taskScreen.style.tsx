import { StyleSheet } from "react-native";

export const TaskScreenStyle = StyleSheet.create({
    container: {
        padding: 30,
        justifyContent: 'space-between',
    },
    button: {
        marginTop: 80
    }
})
